# Telegram Bot API token from @BotFather
TELEGRAM_BOT_TOKEN=

# Deepgram API key for voice transcription
DEEPGRAM_API_KEY=

# Path to vault directory
VAULT_PATH=./vault

# JSON array of Telegram user IDs allowed to use the bot (empty = allow all)
ALLOWED_USER_IDS=[123456789]

# Note: Apple Reminders and Apple Notes are accessed via MCP (no API keys needed)
# They use native macOS system access through mcp-server-apple-events and mcp-apple-notes
